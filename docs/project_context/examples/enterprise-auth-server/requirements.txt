# Enterprise MCP Server following MCP Registry Gateway patterns
# Core FastMCP framework
fastmcp>=0.9.0

# Authentication and security (Azure OAuth patterns)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Optional: For full project integration
# Install the MCP Registry Gateway package for complete functionality:
# pip install mcp-registry-gateway

# Development dependencies (optional)
# pytest>=7.0.0
# ruff>=0.1.0
# mypy>=1.0.0