# Backend API Configuration
# URL to the backend API server (Python/FastAPI)
BACKEND_URL=http://localhost:8000

# Better Auth Configuration
# Generate with: npx @better-auth/cli secret
BETTER_AUTH_SECRET=your-better-secret-key
BETTER_AUTH_URL=http://localhost:3000

# OAuth Providers (all optional - enable only what you need)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Microsoft/Azure SSO (Enterprise SSO support)
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret
AZURE_TENANT_ID=common  # Use 'common' for personal + work accounts, or your tenant ID for org-only

# PostgreSQL Configuration (Frontend)
# Standard PostgreSQL connection string with SSL mode
DATABASE_URL=postgresql://mcp_user:mcp_password@localhost:5432/mcp_registry?sslmode=disable

# Explicit SSL override (optional) - takes precedence over sslmode parameter
# Valid values: true, false
# Leave unset to use sslmode parameter from DATABASE_URL
DB_SSL=false

RESEND_API_KEY=your-resend-api-key

# Frontend Logger Configuration
# Frontend Log level: debug, info, warn, error

# Client-side logging (warn level for production to reduce noise)
NEXT_PUBLIC_LOG_LEVEL=warn
# Server-side logging (info level recommended for production)
LOG_LEVEL=info

# Frontend Enable logging in production (default: false)
NEXT_PUBLIC_LOG_PRODUCTION=true
LOG_PRODUCTION=true

# Frontend Enable logging in browser (default: true in development)
NEXT_PUBLIC_LOG_BROWSER=false

# Frontend Enable structured logging with JSON output (default: true on server)
NEXT_PUBLIC_LOG_STRUCTURED=true
LOG_STRUCTURED=true